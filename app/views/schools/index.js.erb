$('#schools-data').empty();
$('#schools-data').html('<%= escape_javascript(render 'schools') %>');
$('#registered-data').html('<%= escape_javascript(render "shared/registered_data", paginate: @schools_paginate) %>');
